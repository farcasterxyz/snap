---
"@farcaster/snap": minor
---

Add `showValue` prop to slider component for displaying the current value next to the label. Fix native item layout collapse (remove flex:1), fix native badge variant fallback, and tighten native component spacing.
